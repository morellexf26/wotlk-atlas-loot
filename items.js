const items = require("../data/items.json");

// const bossDropItems = items.filter(item => item.source && item.source.category == "Boss Drop");

// const getBossDropItems = (boss) => bossDropItems.filter(dropItem => dropItem.source.name == boss);

// const getBosses = (zone) => bossDropItems.filter(dropItem => dropItem.source.name && dropItem.source.zone == zone);
  

// const arr = Array.from(new Set(getBosses(3456).map(boss => boss.source.name)))
// console.log(arr)

// console.log(items.filter(item => item.name == "Armageddon"))